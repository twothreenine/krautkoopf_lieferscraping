aktuell:
- Verbesserungen:
	- Authentifizierung mit Martins Lösung als Option ergänzen (Routing ändern auf /<fc>/cs, Root-Seite reservieren auf app.foodcoops.at/coopscripts ?)
		- Foodsoft-URL als solche benennen & speichern als Vorbereitung auf mögliche andere Anbindungen wie FCS: Connector type "foodsoft" o.ä. + Version? z.B. fcat deployment
	- "Ausführung läuft" - verhindern, dass laufende Methode noch mal aufgerufen wird und dann nicht mehr in next_possible_methods ist (sperren + prüfen ob noch verfügbar für den Fall, dass ein alter Tab verwendet wird)
	- Instanz-Einstellungen-bearbeiten-Seite, Instanz-löschen-Option
	- Config-Kategorien (ausklappbar? auf Startseite der Instanz)
	- Besser mit hochgeladenen Dateien umgehen
	- Fileupload in Konfiguration ermöglichen, manual changes als extra yaml oder csv
	- Konfiguration-Settings wie "letzte x Ausführungen auflisten" von Config trennen?
	- Daten exportieren & importieren?
	- Aufbau anpassen (Labels und Inputs geordneter)
	- Message Flashing: Farbig hinterlegen (in Box) je nach Kategorie https://flask.palletsprojects.com/en/3.0.x/patterns/flashing/
	- Locales erweitern
	- "Foodcoop" in "Instance" oder "Usergroup" umbenennen?
	- Bug mit Permission Error bei Umbenennen einer Konfiguration (Ordner) fixen? --> vorerst disabled
	- Instance -> script_data, common_data?
	- Skript-Ausführung erst anlegen, wenn erste Methode ausgeführt wird -> "leere" Ausführungen vermeiden, Usecase z.B. Lagerbestand nachschauen
	- Permission-System:
		- Permissions aus Foodsoft auslesen: Bestellgruppe, Arbeitsgruppenmitgliedschaften und Zugriff
		- Skript-Methoden können aus technischen Gründen fix bestimmte Rechte und/oder Bestellgruppe erfordern (foodsoft_finance etc.)
		- Pro Instanz: Welche Permission(s) berechtigen jeweils zum
			- Skript-Konfigurationen ...
				anlegen
				löschen
				Skript wechseln (nur in Ausnahmefällen sinnvoll - extra Menü?)
				(umbenennen)
			- Kategorien ...
				anlegen
				löschen
			- Instanz löschen
		- Pro Skript-Konfiguration: Welche Permission(s) berechtigen jeweils zum
			- Konfiguration sehen
			- Konfiguration bearbeiten
			- letzte Ausführung(en) sehen
			- Ausführung starten
			- jeweilige Callable Methods ausführen
	- Ignore-Feature für Produzenten, Herkunft und Herkunft-startet-mit (z.B. PLZ 2...)

- Skript-Ideen: 
	- Pranger-Rechnung/Lieferschein einlesen
	- Einkistln
	- BerSta und andere wichtige Skripte für andere FCs (auf Honorarbasis anbieten)

	- Cafe Libertad ... auch jeweils Skript um Bestellung abzuschicken? auch für Pranger?
	in Verbindung mit neuem Taskrotation:
	- Pranger-Skript jeden Donnerstag um 3:00 Uhr selbstständig ausführen (Webshop auslesen)

Deployment:
- Commits in foodcoopsat-Repo übertragen
- data hochladen?

in Foodsoft ändern?
- Artikel können per CSV nicht auf inaktiv gesetzt, sondern nur gelöscht werden
- bei bestimmten Sachen auf Hackathon-Fork warten?

in allen Skripten ersetzen:

base.read_in_config(config, 
config.get(

[sm for sm in run.next_possible_methods if sm.name == method][0] -> next(...) (inkl. if ... davor)
